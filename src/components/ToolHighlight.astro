---
interface Props {
  forceDisplay?: boolean;
}

const { forceDisplay = false } = Astro.props;
---

<section class={`py-24 ${forceDisplay ? 'bg-gray-100 dark:bg-[#161617]' : 'bg-gray-100 dark:bg-[#161617]'} transition-colors duration-300`}>
  <div class="container mx-auto px-6">
    <div class="flex flex-col lg:flex-row items-center gap-16">
      <div class="lg:w-1/2">
        {!forceDisplay && (
          <div class="mb-4">
            <span class="text-sm font-medium text-green-600 dark:text-green-400 bg-green-100 dark:bg-green-900/30 px-3 py-1 rounded-full">Featured</span>
          </div>
        )}
        <div class="flex items-center gap-4 mb-4">
          <img src="/jsv.svg" alt="JSON Smart Viewer Logo" class="w-12 h-12" />
          <h2 class="text-4xl font-semibold text-gray-900 dark:text-[#f5f5f7] transition-colors duration-300">JSON Smart Viewer</h2>
        </div>
        <p class="text-gray-700 dark:text-[#86868b] text-lg mb-8 leading-relaxed transition-colors duration-300">
          It serves as a powerful JSON editor, viewer, and comparison tool. Built with a modern React and TypeScript stack, it delivers ultra-fast data exploration without ever sending your data to a server.
        </p>

        <ul class="space-y-4 mb-10">
          {[
            '100% Client-side processing (Privacy First)',
            'Intelligent JSON Comparison and Diff Tool',
            'Save and Restore session directly to disk (Privacy First)',
            'Visualize entire JSON or selected nodes',
            'Handle multiple files at one time',
            'Quick search, bookmark, and notes',
            'Query and filter your data'
          ].map((item) => (
            <li class="flex items-center text-gray-900 dark:text-[#f5f5f7] transition-colors duration-300">
              <svg class="w-5 h-5 mr-3 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M5 13l4 4L19 7"></path></svg>
              {item}
            </li>
          ))}
        </ul>

        <a
          href="https://jsonsv.yakroh.com"
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center px-6 py-3 bg-green-600 hover:bg-green-700 dark:bg-green-500 dark:hover:bg-green-600 text-white font-medium rounded-full transition-all text-base"
        >
          Launch JSON Smart Viewer
        </a>
      </div>

      <div class="lg:w-1/2 relative">
        <div class="relative rounded-2xl overflow-hidden shadow-2xl border border-gray-200 dark:border-white/10 group bg-gray-200 dark:bg-[#1d1d1f] transition-colors duration-300">
          <img
            src="/jsonsv-light.png"
            alt="JSON Smart Viewer Preview"
            class="w-full h-auto object-cover opacity-80 group-hover:opacity-100 transition-opacity duration-500 dark:hidden"
          />
          <img
            src="/jsonsv-dark.png"
            alt="JSON Smart Viewer Preview"
            class="w-full h-auto object-cover opacity-80 group-hover:opacity-100 transition-opacity duration-500 hidden dark:block"
          />
        </div>
      </div>
    </div>
  </div>
</section>